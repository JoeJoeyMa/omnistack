{
  "name": "omnistack-template",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo dev --parallel",
    "dev:web": "turbo dev --filter=@omnistack/web",
    "dev:server": "turbo dev --filter=@omnistack/server",
    "dev:config": "turbo dev --filter=@omnistack/config-ui",
    "dev:native": "turbo dev --filter=@omnistack/native",
    "build": "turbo build",
    "check-types": "turbo check-types",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @omnistack/server db:generate",
    "db:push": "pnpm --filter @omnistack/server db:push",
    "db:studio": "pnpm --filter @omnistack/server db:studio"
  },
  "devDependencies": {
    "turbo": "2.8.12",
    "typescript": "5.9.3"
  }
}